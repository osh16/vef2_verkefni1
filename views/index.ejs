<%- include('header') -%>
	<div class="container">
		<% for (var i = 0; i < categories.length; i++) { %>
			<h2><%- categories[i].title %></h2>
			<div class="row">
				<% for (var j = 0; j < categories[i].videos.length; j++) { %>
					<div class="video col-sm-4">
						<a href="/<%=videos[j].id %>">
							<div class="video_poster">
								<img src="<%- videos[j].poster %>"></img>
								<div class="timestamp"><%= locals.time.duration(videos[j].duration) %></div>
							</div>
							<div class="video_info">
								<h3><%- videos[j].title %></h3>
								<div class="age"><%= locals.time.age(videos[j].created) %></div>
							</div>
						</a>
					</div> <!-- col end -->
				<% } %>
			</div> <!-- row end -->
		<% } %>
	</div> <!-- container end -->
<%- include('footer') -%>
